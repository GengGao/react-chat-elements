import React from 'react';
import { SideBar } from './SideBar';

export default { title: 'SideBar' };

export const Default = () => (
  <SideBar top="Top" center="Center" bottom="Bottom" type="light" />
);
